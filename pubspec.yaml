name: betuko_offline_sync
description: "Ultra-simple offline-first Flutter package. Just get(), save(), and syncAll(). Automatic local storage with Hive, manual sync when you want."
version: 3.3.0
homepage: https://github.com/betuko37/online_offline
repository: https://github.com/betuko37/online_offline
issue_tracker: https://github.com/betuko37/online_offline/issues
documentation: https://github.com/betuko37/online_offline#readme

topics:
  - offline-first
  - sync
  - hive
  - local-storage
  - background-sync

environment:
  sdk: ^3.8.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  http: ^1.2.0
  connectivity_plus: ^6.0.5
  path_provider: ^2.1.0
  workmanager: ^0.9.0+3
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.8
  mockito: ^5.4.4

flutter:
